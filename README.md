# Chirpstack MQTT examples

Python test scripts for subscribing or publishing via MQTT to a Chirpstack network server with simple authentication.

### Requirements

- [paho](https://pypi.org/project/paho-mqtt/).